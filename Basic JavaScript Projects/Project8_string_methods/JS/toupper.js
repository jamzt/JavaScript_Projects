function toUpperCase() {
    var Original = "All work and no play makes Johnny a dull boy.";
    var UpperCase = Original.toUpperCase(); // converts the string to upper case
    document.getElementById("Upper").innerHTML = UpperCase;
}